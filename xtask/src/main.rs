//! xtask — dev tasks (§14): regen schemas, seed data. Run via `cargo xtask <task>`.

fn main() {
    // Scaffold — task subcommands land here.
    println!("xtask — dev tasks. Not yet implemented.");
}
