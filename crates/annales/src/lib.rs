//! Annales — the fact tense of Actio (§8.6). Append-only, what happened — the
//! purest expression of I1. Named series (one file per log), keyed by `YYMMDD`.
//! Not an audit sink: Auspex provenance rides on records (`source`), not here.
//! Build order step 2 — the simplest core (§16). Scaffold.
