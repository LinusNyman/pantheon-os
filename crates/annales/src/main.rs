//! `ann` — Annales' CLI/TUI (§7). stdout is JSON when piped, a table on a TTY.

fn main() {
    // Scaffold — see PANTHEONOS-SPEC.md §8.6. Prints help until implemented.
    println!("ann — Annales (actio · fact). Not yet implemented.");
}
