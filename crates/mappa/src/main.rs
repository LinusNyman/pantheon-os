//! `map` — Mappa's CLI/TUI (§7). stdout is JSON when piped, a table on a TTY.

fn main() {
    // Scaffold — see PANTHEONOS-SPEC.md §8.2. Prints help until implemented.
    println!("map — Mappa (locus · where). Not yet implemented.");
}
