//! Mappa — locus places (where) (§8.2). Places as a kind-partitioned register,
//! each entity one `.json` carrying coordinates/address/URL in `data`.
//! Default-homed under `cl*`. Referenced as `mappa:<slug>`. Scaffold.
