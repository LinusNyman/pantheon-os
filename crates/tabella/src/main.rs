//! `tab` — Tabella's CLI/TUI (§7). stdout is JSON when piped, a table on a TTY.

fn main() {
    // Scaffold — see PANTHEONOS-SPEC.md §8.7. Prints help until implemented.
    println!("tab — Tabella (prose · meaning). Not yet implemented.");
}
