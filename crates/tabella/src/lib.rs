//! Tabella — documents (meaning) (§8.7). Prose: notes, quotes, principles, homed
//! in Anima. Not a store of its own — handles every loose `[code]_*.md` in place.
//! Home is the path; `+++` TOML frontmatter carries `type` and `tags` only.
//! Scaffold.
