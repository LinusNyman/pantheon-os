//! `pan` — the structural CLI/TUI (§5.5, §10). Works one layer down from `data`:
//! codes, files, refs, node annotations.

fn main() {
    // Scaffold — see PANTHEONOS-SPEC.md §5.5 / §10. Prints help until implemented.
    println!("pan — Pantheon (the frame). Not yet implemented; see docs/src/PANTHEONOS-SPEC.md.");
}
