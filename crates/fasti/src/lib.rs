//! Fasti — the placement tense of Actio (§8.4). Things on the timeline, as named
//! series (one file per timeline). Records carry an in-record `kind`: `spn` a
//! span (from/to), `evt` an event (a point). A calendar view composes Album's
//! `data.away` on read, never copied in (I3). Scaffold.
