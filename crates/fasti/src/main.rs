//! `fas` — Fasti's CLI/TUI (§7). stdout is JSON when piped, a table on a TTY.

fn main() {
    // Scaffold — see PANTHEONOS-SPEC.md §8.4. Prints help until implemented.
    println!("fas — Fasti (actio · placement). Not yet implemented.");
}
