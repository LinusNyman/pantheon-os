//! Tessera — the tiles (§11.2). Small widgets that fold core JSON into a present
//! (a clock, a GPA, net worth) and render through Porticus. A tile never stores
//! its value; it recomputes from the latest readings each frame (I1). Scaffold.
