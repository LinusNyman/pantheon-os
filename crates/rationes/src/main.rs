//! `rat` — Rationes' CLI/TUI (§7). stdout is JSON when piped, a table on a TTY.

fn main() {
    // Scaffold — see PANTHEONOS-SPEC.md §8.3. Prints help until implemented.
    println!("rat — Rationes (res · what). Not yet implemented.");
}
