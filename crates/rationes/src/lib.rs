//! Rationes — res holdings (what) (§8.3). Partitioned; kinds `acc`/`ast`/`cla`.
//! `acc`/`ast` may carry a companion balance series (`[node]__bal__<slug>.jsonl`);
//! `cla` has none. Net worth folds the latest balances, never stored (I1).
//! Default-homed under `cr*`, not sphere-locked. Referenced as `rationes:<slug>`.
//! Scaffold.
