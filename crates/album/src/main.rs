//! `alb` — Album's CLI/TUI (§7). stdout is JSON when piped, a table on a TTY.

fn main() {
    // Scaffold — see PANTHEONOS-SPEC.md §8.1. Prints help until implemented.
    println!("alb — Album (societas · who). Not yet implemented.");
}
