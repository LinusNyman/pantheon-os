//! Album — societas agents (who) (§8.1). People and organizations, stored as a
//! kind-partitioned register (filename kinds `ind`/`org`/`grp`). Referenced
//! everywhere as `album:<slug>`. Default-homed under `cs*`, not sphere-locked
//! (§8.3). Scaffold.
