//! `pen` — Pensum's CLI/TUI (§7). stdout is JSON when piped, a table on a TTY.

fn main() {
    // Scaffold — see PANTHEONOS-SPEC.md §8.5. Prints help until implemented.
    println!("pen — Pensum (actio · intention). Not yet implemented.");
}
