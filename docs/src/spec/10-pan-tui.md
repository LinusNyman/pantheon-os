## 10. `pan` — the structural TUI

Bare `pan` opens a TUI. `pan` never touches `data` — it works one layer down, on the tree itself: codes, files, refs, node annotations. Two tabs; annotate is an action on the selected node (§10.3).

### 10.1 Tree

Browse the ontology and act on it. Left pane: codes, symbols, labels, child and record counts. Right pane: the selected node's meta, which cores have files there, which rules are scoped there.

| Verb | Does |
|---|---|
| `pan new <parent> <char> <label>` (or `--def <definition>` for a char-less node, §5.1) | `mkdir` + scaffold the meta dir (and an empty `[code]__.toml` for top layers); accepts a batch of siblings in one plan (I7) |
| `pan rename <code> [--char C] [--label L]` (a definition-prefix node has no char — `--def D` re-derives its definition, code, and entity-node slug) | change defining char and/or label |
| `pan rename-prefix <old> <new>` / `pan rename-pattern <from> <to>` | bulk name fix over the subtree — rewrite a code-prefix, or substitute a literal across labels/filenames/slugs/series names; a slug or series-name hit cascades its refs (§5.4), and a code-prefix hit cascades the rule headers naming it (§9.2), exactly as `rename` and `mv` do |
| `pan mv <code> --to <parent>` | re-home a node |
| `pan merge <src> --into <dst>` | union two branches — dissolve `src` into `dst`, merging a child both sides hold rather than renaming one onto the other; every file collision is refused and listed (§5.3, §5.5) |
| `pan mv-file <file>… --to <code>` | re-home files — a misfiled record, a whole series, a document, homed bulk (§7.2, §6.5) — rewriting each `[code]` prefix that is there to rewrite; many sources, one plan |
| `pan rm <code>` | delete — refused if the node has children, record files, **or rules**: a rule is neither stratum (§6.5) but it is code you authored and granted, and the last thing that should drop without a word |

`rename`, `mv`, `merge`, and `rename-prefix` cascade — a node's code is its path (I3), so every child directory name and file prefix under the branch, and every rule header naming the code (§9.2), must change. **All four walk the node tree and nothing else** (§6.3): a directory that is neither a node nor a meta dir — homed bulk, a project's `.git`, a build tree — rides along inside its parent, unentered and unrenamed. No ignore file governs that bound and none may (§13, §18); the tree's own shape is the whole of it. **Stored entity refs do not** — they carry no path, so nothing elsewhere in the tree points at the old code (§5.4); this is exactly what location-independence buys. The cascade is therefore contained to the moved branch plus rule headers. **One exception:** renaming a **definition-prefix** node re-derives its entity-node's slug (the slug *is* the definition, §5.1), so unlike a triple-node rename it also **cascades that entity's `core:slug` refs** (§5.4) — the single node-rename that touches refs. Both run as a **planned transaction**:

1. `--dry-run` (default in the TUI) computes the full plan — every directory rename, prefix change, and affected rule header, plus warnings — and shows it before anything moves.
   **A plan that would overwrite anything is refused, and every collision is named at once**, so one review answers for the whole plan rather than surfacing the next problem each time the last is cleared. `rename` replaces what sits at its destination, and a recode plans one rename per file whose prefix changes — so this is the guard between a stray file and silent destruction, and §18 keeps no undo behind it. There is no `--force`: what is in the way is moved aside by hand, which leaves it on disk. The check is asked again immediately before each rename, because the plan token cannot close that window — the token is checked against a freshly *computed* plan, recomputed from the same tree, so a file created in between is invisible to both.
2. Nothing is written outside the tree — there is no cache dir and no in-flight plan file (§18). If the process dies mid-operation, the half-finished rename is diagnosed directly from **the tree's current state** via `pan validate` (§10.2), whose candidate fixes resolve it; there is no automatic resume and nothing to read back. The tree's current state is the only truth (I1): a partial rename is read from what *is*, never from a log of what was intended.
3. Nothing is locked either. §6.4's lock is a record file's, and §18 leaves nowhere to keep a bigger one — so a core writing a record inside the branch this operation is moving is a race the tools do not close. One hand moves the tree at personal scale, and what a race would leave is what a crash leaves: a state read back from what *is* (§10.2), not a guarantee bought with a lock the system has no room for.

Confirmation follows the hardcoded mutation rule (§7.3): the structural transaction always confirms before it moves anything.

### 10.2 Validate

`pan validate`'s findings, browsable. **Where a finding has exactly one legal correction, `pan` applies it** — an uppercase char, a space or `-` in a name, a stray punctuation mark, a non-NFC form all normalize to a single answer (§5.1), so `pan` fixes them directly. This is not a violation of "`pan` doesn't decide": auto means *there is no choice to make*, not *no confirm* — the fix is still a mutation and confirms first (§7.3). **Where a finding has a genuine choice, `pan` never picks** — it surfaces **candidate commands** to review and run, and stops at "here's what I found and the fixes that would resolve it." Diagnosis is from the tree's current state — there is no transaction history to consult (§10.1), so `pan` reads what *is* and offers the corrections that make it consistent, rather than recalling what a past rename intended.

A finding names the inconsistency and its candidate fixes; where more than one correction is plausible, all are shown and the user picks. A `csa_`-prefixed file inside `cso__/`, for instance — misfiled, or a rename that missed it? — surfaces both `pan mv-file … --to csa` (it belongs at `csa`) and `pan rename-prefix csa cso cso` (it belongs here, wrong prefix), and you choose which is true. The tree can't tell you which was *meant*; you can.

Entity refs (`album:mara`) never dangle on an ordinary (triple) node rename — they carry no path, so re-homing or renaming such a node leaves them untouched (I3); the only thing that rename can leave inconsistent is a *filename prefix* under the moved branch. The **one exception is a definition-prefix node**, whose slug *is* its definition (§10.1): renaming it cascades the entity-node's `core:slug` refs like a record rename, so a crash mid-cascade can strand some on the old slug — diagnosed by `pan validate` exactly as a crashed rename is (§5.4, §7.2). Otherwise a ref dangles only when its target record — an entity, document, task, or named series — is deleted, which `pan validate` reports for the user to fix at the source.

Scope is strictly the tree: every ref resolves; every file sits at a real node with a matching `[code]` prefix; code collisions (reserved-verb and `root` clashes, §5.5, §7.3, and a definition-prefix that prefix-shadows a sibling lineage, §5.3); slug and hand-named-series-name collisions across nodes (soft — reported for you to fix at the source, §5.4); **a directory under a node that is neither its meta dir nor a legal child** (§5.1) — the orphan a crashed rename leaves, `csa__/` stranded beneath a node now named `csb`, and the one finding that cannot inherit the fold's filter to see it: the walk that folds records visits node dirs and meta dirs only (§6.3), so the orphan is invisible to it and to every core, and `validate` therefore descends what the fold declines. That is the only walk in the system that does, and §13's totality is the fold's claim, never this one's; prefix/directory mismatches; a kind token owned by no installed core (soft — a typo, or a core not installed, §5.5); a rule header naming a dead code; a determined-name series stranded at a node its determinant has left — a `Series { named: false }` carrying a name (a `balance`) whose co-located entity is gone, which the spine can see because `named` told it the name is a determinant to match (§7.1); §7.2 moves the two together, but a hand's `mv` can part them; a def-prefix node with a non-def-prefix child (whose code would be the ambiguous concatenation §5.1 forbids); and more than one slug-less self-record (`[code]__[kind].json`) at one node (leaving `album:<def>` no single answer). The last two are genuine-choice fixes — `pan` shows candidates and never auto-applies, since the definition to use, or the node's true kind, is yours to name. A record whose `data` fails its core's schema is out of scope — `pan` doesn't inspect `data`.

### 10.3 Annotate

Edit the selected node's `[code]__.toml` (§5.2) — symbol, keywords, deity, explanation — in place via `toml_edit`, so hand-written comments and ordering survive (the CLI equivalent is `pan annotate`, §5.5). This is the only hand-written configuration left in the system (UI, confirmation, and core defaults are all hardcoded — §6.6), and it annotates the ontology rather than tuning behavior.
